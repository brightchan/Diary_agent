interface:
  display_name: "生活日记与想法管家"
  short_description: "记录日记、想法与决策，并保存独立的 Agent 反馈"
  default_prompt: "Use $record-life-journal to capture this entry and prepare its separate Agent feedback for confirmation."

policy:
  allow_implicit_invocation: true
